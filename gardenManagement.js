
const temperature = 90;
const timeOfDay = "morning";
let soilMoisture = 30;

function controlWatering(temperature) {
temperature = 90

    if ( temperature > 80) 
        console.log( "Watering on" ) 
    
    else 
        console.log( "Watering off" ) 
controlWatering()

}


function timeOfDay ( ) {
    timeOfDay= morning
    if (timeOfDay = evening || night)
        console.log( "Lights on" ); 
    
    else {
        console.log ( "Lights off" );
    }
timeOfDay()
}

 function soilMoistureAdjustment ( soilMoisture) {
 soilMoisture = 30

    while ( soilMoisture < 40 ) 
        console.log( soilMoisture );
        soilMoisture = soilMoisture + 5
    
    console.log(soilMoisture)
    let soilMoisture = 30
soilMoistureAdjustment()
 }